#include <iostream>
#include <fstream>

int main(int argc, char const *argv[])
{
	// instanciar Cluster(dimension_matriz)
	// llenar el Cluster de dimension_matriz**2 instancias de Cell
	// Cell es una clase que tiene un state (vivo o muerto) y un puntero a un rectangulo
	// el color del rectangulo cambia dependiendo de si la celula está viva o muerta
	return 0;
}